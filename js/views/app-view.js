/*global Backbone, _ */
var app = app || {};

(function () {
	'use strict';

	app.AppView = Backbone.View.extend({

		el: '#app',

		events: {
		},

		initialize () {
		},

		render () {
		}

	});
})();
